docker-compose up -d nginx mysql redis workspace vessel - поднять докер
docker-compose exec workspace bash - войти в рабочее пространство
cp env-example .env
composer install
php artisan ui bootstrap --auth
npm install
npm update
npm run dev
php artisan migrate --seed

